on:
  cli:
    init:
      commit-sha: ${{ event.git.sha }}
  github:
    push:
      if: ${{ event.git.branch == 'main' }}
      init:
        commit-sha: ${{ event.git.sha }}
    pull_request:
      init:
        commit-sha: ${{ event.git.sha }}

tasks:
  - key: tests
    parallel:
      matrix:
        ruby-version: ["3.1.7", "3.2.9", "3.3.10", "3.4.9", "4.0.3"]
    call: ${{ run.dir }}/test.yml
    init:
      commit-sha: ${{ init.commit-sha }}
      ruby-version: ${{ parallel.ruby-version }}
